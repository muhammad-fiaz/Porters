# Porters Package Manager for C/C++

Porters is a modern package manager for C and C++, built with Rust. It provides a streamlined way to manage dependencies, build configurations, and project metadata using a **TOML-based configuration file** (`porters.toml`).
